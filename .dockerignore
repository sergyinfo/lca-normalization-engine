node_modules
*/node_modules
**/node_modules
.next
*/*/.next
**/.next
.env
.env.local
.git
.DS_Store

# Build artefacts
*.tsbuildinfo
**/*.tsbuildinfo

# Don't copy the mutable keys DB into the analytics-web build context.
apps/analytics-web/data/keys.db
apps/analytics-web/data/keys.db-*

# Local LCA archive — never belongs in any image.
data/*.xlsx
